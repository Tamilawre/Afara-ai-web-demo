[tool.poetry]
name = "afara-ai-web-ui"
version = "0.1.0"
description = "Add your description here"
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"